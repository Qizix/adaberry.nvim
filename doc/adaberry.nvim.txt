*adaberry.nvim.txt*       For Neovim >= 0.8.0      Last change: 2026 January 13

==============================================================================
Table of Contents                             *adaberry.nvim-table-of-contents*

1. Prerequisites                                  |adaberry.nvim-prerequisites|
2. Installing                                        |adaberry.nvim-installing|
  - Using packer                        |adaberry.nvim-installing-using-packer|
  - Using lazy.nvim                  |adaberry.nvim-installing-using-lazy.nvim|
  - Using vim-plug                    |adaberry.nvim-installing-using-vim-plug|
3. Basic Usage                                      |adaberry.nvim-basic-usage|
4. Configuration                                  |adaberry.nvim-configuration|
  - Overriding                         |adaberry.nvim-configuration-overriding|
>
      <h1> <img src="https://i.postimg.cc/WpQzgxVh/plugin-Icon.png" width="80px"><br/>adaberry.nvim</h1>
     </div>
<


A custom dark colorscheme for Neovim with treesitter and |semantic highlights|
support.


==============================================================================
1. Prerequisites                                  *adaberry.nvim-prerequisites*

Neovim 0.8.0+


==============================================================================
2. Installing                                        *adaberry.nvim-installing*


USING PACKER                            *adaberry.nvim-installing-using-packer*

>lua
    use { "adaberry/adaberry.nvim" }
<


USING LAZY.NVIM                      *adaberry.nvim-installing-using-lazy.nvim*

>lua
    { "adaberry/adaberry.nvim", priority = 1000 , config = true, opts = ...}
<


USING VIM-PLUG                        *adaberry.nvim-installing-using-vim-plug*

>vim
    Plug 'adaberry/adaberry.nvim'
<


==============================================================================
3. Basic Usage                                      *adaberry.nvim-basic-usage*

Inside `init.vim`

>vim
    set background=dark
    colorscheme adaberry
<

Inside `init.lua`

>lua
    vim.o.background = "dark"
    vim.cmd([[colorscheme adaberry]])
<


==============================================================================
4. Configuration                                  *adaberry.nvim-configuration*

Additional settings for adaberry are:

>lua
    -- Default options:
    require("adaberry").setup({
      terminal_colors = true, -- add neovim terminal colors
      undercurl = true,
      underline = true,
      bold = true,
      italic = {
        strings = true,
        emphasis = true,
        comments = true,
        operators = false,
        folds = true,
      },
      strikethrough = true,
      invert_selection = false,
      invert_signs = false,
      invert_tabline = false,
      inverse = true, -- invert background for search, diffs, statuslines and errors
      contrast = "", -- can be "hard", "soft" or empty string
      palette_overrides = {},
      overrides = {},
      dim_inactive = false,
      transparent_mode = false,
    })
    vim.cmd("colorscheme adaberry")
<

**VERY IMPORTANT**: Make sure to call setup() **BEFORE** calling the
colorscheme command, to use your custom configs


OVERRIDING                             *adaberry.nvim-configuration-overriding*


PALETTE ~

You can specify your own palette colors. For example:

>lua
    require("adaberry").setup({
        palette_overrides = {
            bright_green = "#990000",
        }
    })
    vim.cmd("colorscheme adaberry")
<


HIGHLIGHT GROUPS ~

If you don’t enjoy the current color for a specific highlight group, now you
can just override it in the setup. For example:

>lua
    require("adaberry").setup({
        overrides = {
            SignColumn = {bg = "#ff9900"}
        }
    })
    vim.cmd("colorscheme adaberry")
<

It also works with treesitter groups and lsp semantic highlight tokens

>lua
    require("adaberry").setup({
        overrides = {
            ["@lsp.type.method"] = { bg = "#ff9900" },
            ["@comment.lua"] = { bg = "#000000" },
        }
    })
    vim.cmd("colorscheme adaberry")
<

Please note that the override values must follow the attributes from the
highlight group map, such as:

- **fg** - foreground color
- **bg** - background color
- **bold** - true or false for bold font
- **italic** - true or false for italic font

Other values can be seen in |`synIDattr`|

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
